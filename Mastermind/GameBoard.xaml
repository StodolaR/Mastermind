<UserControl x:Class="Mastermind.GameBoard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mastermind"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="400">
    <Grid>
        <Canvas x:Name="board" Width="350" Height="470">
            <Border BorderThickness="2,0,2,10" BorderBrush="Gray">
                <Rectangle Width="350" Height="470" Fill="LightGray"/>
            </Border>
            <Path Data="M 0 -20 L 155 -20 L 160 -50 L -5 -50 Z" Fill="Gray" Panel.ZIndex="1" Canvas.Left="20" Canvas.Top="60" Style="{StaticResource coverStyle}"/>
            <Path Data="M -5 0 L 160 0 L 155 -20 L 0 -20 Z" Fill="DarkGray" Panel.ZIndex="1" Canvas.Left="20" Canvas.Top="60" Style="{StaticResource coverStyle}"/>
            <Path Data="M -5 0 L 0 -20 L -5 -50 L -10 -30 Z" Fill="DimGray" Panel.ZIndex="1" Canvas.Left="20" Canvas.Top="60" Style="{StaticResource coverStyle}"/>
            <Path Data="M 160 0 L 165 -30 L 160 -50 L 155 -20 Z" Fill="DimGray" Panel.ZIndex="1" Canvas.Left="20" Canvas.Top="60" Style="{StaticResource coverStyle}"/>
        </Canvas>
    </Grid>
</UserControl>
